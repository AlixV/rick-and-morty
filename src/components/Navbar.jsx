import React from "react";
import { NavLink } from "react-router-dom";

const Navbar = () => {
  return (
    <nav>
      <NavLink to="/">Rick and Morty App</NavLink>
    </nav>
  );
};

export default Navbar;
